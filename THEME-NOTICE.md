# Theme notice

Visual foundation: `rampatra/photography`, a Jekyll photography portfolio based on HTML5 UP Multiverse, distributed under GPL-3.0. This repository loads the theme with `jekyll-remote-theme` and adds a Persian RTL adaptation layer.
