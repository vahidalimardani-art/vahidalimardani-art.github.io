# نصب تمیز

1. از مخزن فعلی نسخه پشتیبان ZIP بگیرید.
2. پوشه‌های `_works`، `_notes` و `media` را نگه دارید.
3. فایل‌ها و پوشه‌های نمایشی قدیمی را حذف کنید: `_layouts`، `_includes`، `assets`، `works`، `notes`، `about`، `index.html`، `404.html` و `_config.yml`.
4. محتوای این بسته را در ریشه مخزن بارگذاری کنید.
5. `.pages.yml` موجود را با نسخه این بسته جایگزین کنید.
6. تنظیم GitHub Pages روی `main` و `/ (root)` بماند.
